# Google Places API (New) key for Canada address autocomplete.
# Without it, address fields work as plain text inputs.
# Set the same variable in Vercel (Project Settings > Environment Variables).
VITE_GOOGLE_MAPS_KEY=
